(ns api.diplomat.http-in.customer)


(defn create-customer [request]
  {:status 200 :body "{}"})

(defn find-all [request]
  {:status 200 :body "{}"})

(defn find-by-id [request]
  {:status 200 :body "{}"})

(defn disable-customer [request]
  {:status 200 :body "{}"})